// temp placeholder for class tests

describe('Input Class', () => {
  it('returns true given valid input', () => {

  });

  it('creates a new instance with both type and payload properties', () => {

  });

  it('returns false given invalid input', () => {

  });
});

describe('Note Class', () => {
  it('returns nothing from execute method if no command given', () => {

  });

  it('returns a note with both id and text properties if command and data are valid', () => {

  });
});